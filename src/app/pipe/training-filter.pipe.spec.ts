import { TrainingFilterPipe } from './training-filter.pipe';

describe('TrainingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
